---
---

<main>
	<a class="back" href="/">go back</a>
	<div id="p5-container"></div>
</main>

<style lang="scss">
	@keyframes slideout {
		from {
			opacity: 0.8;
			transform: translateY(0);
		}
		to {
			transform: translateY(-40px);
		}
	}
	body {
		padding: 0;
	}
	main {
		top: 0;
		bottom: 0;
		left: 0;
		right: 0;
		width: 100svw;
		height: 100svh;
		margin: 0;
		padding: 0;
		background: white;
		color: black;
		overflow: hidden;
	}
	.back {
		position: absolute;
		margin-left: 20px;
		font-family: "Rethink Sans";
		font-size: 46px;
		font-weight: 500;
		color: var(--text-50);
		transition: all 0.4s;
		transform: translateY(0);
		opacity: 0.05;
		animation: ease-in 1s 3s forwards slideout;

		&:hover {
			opacity: 0.8;
			text-shadow: 4px 2px 20px var(--background-300);
			transform: translateY(40px);
			transition: transform 0.4s;
			animation-direction: reverse;
			animation-play-state: paused;
			// animation-play-state: running;
			// animation: ease-in 1s reverse both slideout;
		}
	}
	#p5-container {
		width: 100%;
		height: 100%;
		box-sizing: border-box;
	}
</style>
